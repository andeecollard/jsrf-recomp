# Python
__pycache__/
*.pyc
*.pyo
.venv/

# Tool output (regenerated per-game)
tools/disasm/output/
tools/func_id/output/
tools/recomp/output/
tools/abi_analysis/output/

# Game files (user provides their own)
game_files/
*.xbe
# ...except the synthetic one the tests default to. It is 4KB of generated
# header and five leaf functions, carries no title content, and without it
# the memory-layout test has nothing to open on a fresh clone.
!tools/conformance/test.xbe
*.iso
*.xiso

# IDE
.vs/
.vscode/
.idea/
*.suo
*.user

# OS
Thumbs.db
.DS_Store

# Build
build/
bin/
*.obj
*.exe
# Out-of-tree build + regeneration output (hundreds of MB, rebuilt by
# diagnostics/jsrf_first_fault/regenerate.sh)
build-macos/

# Runtime logs
xbox_kernel.log

# Out-of-tree Windows build (hundreds of MB, same as build-macos)
build-windows-jsrf/

# RECOMP_ICALL_FEEDBACK output, rewritten every run
icall_targets.dump

# Stray tool output directory
output/

# Generic split-tool output
tools/split/output/

# Claude Code session state and agent worktrees
.claude/
